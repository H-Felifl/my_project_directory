{% extends 'base.html.twig' %}

{% set page_title = 'order' %}


{% block inhoud %}
    <div class="container">
        <div class="row">
            <div class="col">
                <div class="card">
                <img class="img-fluid" src="/{{ product.img }}" alt="">
                </div>
            </div>
            <div class="col">
        <h1>{{ product.title }}</h1>
        {{ form(form) }}
            </div>

        </div>
    </div>

{% endblock %}